# WebSocket URL of the backend. Leave empty for local dev
# (falls back to ws://localhost:4000). Use wss:// in production.
VITE_WS_URL=

# REST API base URL of the backend. Leave empty for local dev
# (falls back to http://localhost:4000). Use https:// in production.
VITE_API_URL=
